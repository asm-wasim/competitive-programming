
#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n,x;

    long long int k;
    vector <int> w;
    for(int i=0; i<n; i++)
    {
        scanf("%d",&x);
        w.push_back(x);
    }
    sort(w.begin(),w.end());
    int si=w.size();
    for(int i=0; i<si; i++)
    {

    }
    return 0;
}
